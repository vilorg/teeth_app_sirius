import 'package:flutter/material.dart';

class SetArea extends StatelessWidget {
  const SetArea({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}